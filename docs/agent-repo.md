# WearOS-WatchFaceFormat 固有ルール

## 概要
WearOS向けウォッチフェイス。DSLでWatch Face Format XMLを生成。

## 構成
- `app/` — Androidアプリ
- `build-logic/` — WFF XML生成用GradleプラグインとDSL

## ビルド
```shell
./gradlew assembleDebug
./gradlew assembleRelease
```

